name: algorithm_visualizer
description: A simple algorithm visualizer project application.

environment:
  sdk: '>=3.6.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^2.4.4
  provider: ^6.1.2

dev_dependencies:
  build_runner: ^2.4.13
  flutter_test:
    sdk: flutter
  freezed: ^2.5.7
  flutter_lints: ^5.0.0

dependency_overrides:
  file: ^7.0.1

flutter:
  uses-material-design: true
